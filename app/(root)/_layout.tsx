import { Stack } from 'expo-router';
import React from 'react';


const Layout = () =>{

  return (
      <Stack>
        <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
      </Stack>
  );
}
export default Layout;

// so. i have a page and a menu to code rn, just as the ui. then i have to do some research with my teammates on an api for our project 

// i will explain what it is 

